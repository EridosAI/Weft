# Core dependencies for Weft (Continuous Trajectory PAM Tier A implementation)
# Pinned versions for reproducibility. See HANDOFF.md for the canonical
# development environment (WSL2, Python 3.12.3, CUDA 12.8 via WSL2 passthrough).
# Install with: pip install -r requirements.txt

# PyTorch stack — cu128 build is what the WSL environment provides via CUDA 12.8 passthrough.
# For machines without matching CUDA, install the matching torch build from pytorch.org first.
torch==2.10.0
torchvision==0.25.0

# Numerics / IO
numpy==2.4.2
pillow==12.1.1
opencv-python==4.13.0

# FAISS — CPU variant; memory bank is on CPU per spec §3.4 device placement.
faiss-cpu==1.13.2

# Environment (Push-T curriculum)
gymnasium==1.3.0
gym-pusht==0.1.6

# V-JEPA 2 loading
transformers==5.3.0

# Logging and experiment tracking
tensorboard==2.20.0
PyYAML==6.0.1
tqdm==4.67.3

# Analysis / plotting
scikit-learn==1.8.0
matplotlib==3.10.8
pandas==2.3.3

# Test runner
pytest==9.0.3
